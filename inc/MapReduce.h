#pragma once

#include <string>

namespace mr {

template<typename M, typename R>
class MapReduce {
  public:

    void process(const std::string& filename, int mnum, int rnum) {

    }
};

} // namespace mr.
