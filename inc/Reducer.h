#pragma once


#include <algorithm>
#include <string>
#include <vector>

namespace mr {

/**
 * @brief Функтор операции свертки.
 */
struct Reducer
{
  auto operator()(const std::string& str) {
    std::vector<std::string> map;

    return map;
  }
};

} // namespace mr.
